# 🤖 Бот для поиска дешевых авиабилетов Москва → Уфа

Автоматический бот, который ищет дешевые авиабилеты и отправляет уведомления в Telegram.

## 📋 Описание

Бот автоматически ищет авиабилеты по заданным параметрам:
- **Маршрут**: Москва (MOW) → Уфа (UFA)
- **Даты**:
  - 26 декабря (вечерние рейсы с 18:00)
  - 27 декабря (любое время)
  - 28 декабря (любое время)
- **Максимальная цена**: 10,000 ₽
- **Уведомления**: Telegram бот

## 🚀 Быстрый старт

### Шаг 1: Подготовка Telegram бота

**ВАЖНО!** Перед запуском бота необходимо:

1. Найдите вашего бота в Telegram по токену `8080110045:AAGK01_8PByIWA-F9o4wJnlGRdQtWu89Uyo`
2. Откройте бота в Telegram
3. Отправьте команду `/start`

Это необходимо для того, чтобы бот смог определить ваш chat_id и отправлять вам уведомления.

### Шаг 2: Установка зависимостей

```bash
# Установите необходимые библиотеки
pip3 install requests python-dateutil
```

Или используйте файл requirements:

```bash
pip3 install -r requirements_flight_bot.txt
```

### Шаг 3: Запуск бота

#### Вариант 1: Через bash-скрипт (рекомендуется)

```bash
# Сделайте скрипт исполняемым
chmod +x start_flight_bot.sh

# Запустите бота
./start_flight_bot.sh
```

#### Вариант 2: Напрямую через Python

```bash
# Непрерывный режим (проверка каждый час)
python3 cheap_flights_bot.py

# Однократный поиск
python3 cheap_flights_bot.py --once
```

## ⚙️ Настройка

Все настройки находятся в файле `flight_config.py`:

### Изменение маршрута

```python
ORIGIN_CITY = "MOW"  # Москва
DESTINATION_CITY = "UFA"  # Уфа
```

### Изменение максимальной цены

```python
MAX_PRICE = 10000  # рублей
```

### Изменение дат поиска

```python
DEPARTURE_DATES = [
    {
        "date": "2024-12-26",
        "time_preference": "evening",  # вечер
        "min_hour": 18,  # с 18:00
        "max_hour": 23   # до 23:59
    },
    # ... добавьте другие даты
]
```

Доступные варианты `time_preference`:
- `"evening"` - вечер
- `"morning"` - утро
- `"afternoon"` - день
- `"any"` - любое время

### Изменение интервала проверки

```python
CHECK_INTERVAL = 3600  # секунды (3600 = 1 час)
```

Рекомендуемые значения:
- 1800 (30 минут)
- 3600 (1 час)
- 7200 (2 часа)

### Изменение Telegram бота

```python
TELEGRAM_BOT_TOKEN = "ВАШ_ТОКЕН"
```

## 📱 Получение chat_id

Бот автоматически определяет ваш chat_id при первом запуске, если вы отправили `/start` боту.

Если бот не может определить chat_id:
1. Отправьте `/start` боту в Telegram
2. Перезапустите бота

Также вы можете указать chat_id вручную в `flight_config.py`:

```python
TELEGRAM_CHAT_ID = "ВАШ_CHAT_ID"
```

Чтобы узнать свой chat_id:
1. Отправьте `/start` боту [@userinfobot](https://t.me/userinfobot)
2. Он покажет ваш ID

## 📊 Логи

Бот ведет подробные логи в файле `cheap_flights_bot.log`.

Просмотр логов:

```bash
# Последние 50 строк
tail -50 cheap_flights_bot.log

# Непрерывный просмотр
tail -f cheap_flights_bot.log
```

## 🗂 Файлы проекта

- `cheap_flights_bot.py` - Главный файл бота
- `flight_finder.py` - Модуль поиска билетов
- `telegram_notifier.py` - Модуль отправки уведомлений в Telegram
- `flight_config.py` - Конфигурация бота
- `found_flights.json` - История найденных билетов (создается автоматически)
- `cheap_flights_bot.log` - Лог-файл (создается автоматически)

## 🔄 Автоматический запуск

### Linux/macOS (cron)

Добавьте в crontab для запуска каждый час:

```bash
# Откройте crontab
crontab -e

# Добавьте строку (замените /path/to на реальный путь)
0 * * * * cd /path/to/lalalala && /usr/bin/python3 cheap_flights_bot.py --once >> cron.log 2>&1
```

### Systemd сервис (Linux)

Создайте файл `/etc/systemd/system/flight-bot.service`:

```ini
[Unit]
Description=Cheap Flights Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/lalalala
ExecStart=/usr/bin/python3 /path/to/lalalala/cheap_flights_bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Запустите сервис:

```bash
sudo systemctl enable flight-bot
sudo systemctl start flight-bot
sudo systemctl status flight-bot
```

### Docker (опционально)

```dockerfile
FROM python:3.9-slim

WORKDIR /app

COPY requirements_flight_bot.txt .
RUN pip install --no-cache-dir -r requirements_flight_bot.txt

COPY cheap_flights_bot.py flight_finder.py telegram_notifier.py flight_config.py ./

CMD ["python", "cheap_flights_bot.py"]
```

Запуск:

```bash
docker build -t flight-bot .
docker run -d --name flight-bot --restart unless-stopped flight-bot
```

## 🛠 Устранение неполадок

### Бот не находит билеты

- API Aviasales может иметь ограничения на бесплатные запросы
- Попробуйте увеличить интервал между проверками
- Проверьте правильность кодов IATA городов

### Не приходят уведомления в Telegram

1. Убедитесь, что вы отправили `/start` боту
2. Проверьте токен бота в `flight_config.py`
3. Проверьте логи: `tail -50 cheap_flights_bot.log`
4. Проверьте подключение к интернету

### Ошибки в логах

Проверьте:
- Установлены ли все зависимости: `pip3 install requests`
- Правильность токена Telegram бота
- Доступ к интернету

## 💡 Советы

1. **Не спамьте API** - используйте интервал не менее 30 минут
2. **Проверьте даты** - убедитесь, что даты в будущем
3. **Тестируйте локально** - сначала запустите с `--once`
4. **Следите за логами** - они помогут найти проблемы

## 🔐 Безопасность

- Не публикуйте токен Telegram бота в открытом доступе
- Храните `flight_config.py` в безопасности
- Добавьте `flight_config.py` в `.gitignore` если используете Git

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи: `cheap_flights_bot.log`
2. Убедитесь, что все зависимости установлены
3. Проверьте настройки в `flight_config.py`

## 📄 Лицензия

Бот создан для личного использования.

## 🎯 Roadmap

Возможные улучшения:
- [ ] Поддержка нескольких маршрутов
- [ ] Веб-интерфейс для настройки
- [ ] Интеграция с большим количеством API
- [ ] Уведомления по email
- [ ] Графики изменения цен

---

**Удачного поиска дешевых билетов!** ✈️
