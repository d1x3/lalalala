# Как найти селекторы на сайте Betr - пошаговая инструкция

## Шаг 1: Откройте сайт Betr

1. Откройте Chrome/Edge/Firefox
2. Перейдите на https://picks.betr.app/picks/home/lobby

## Шаг 2: Откройте DevTools (Инструменты разработчика)

**Способ 1:** Нажмите `F12` на клавиатуре
**Способ 2:** Правой кнопкой мыши → "Проверить" (Inspect)
**Способ 3:** `Ctrl + Shift + I` (Windows/Linux) или `Cmd + Option + I` (Mac)

Откроется панель внизу или справа с кодом страницы.

## Шаг 3: Включите инспектор элементов

В DevTools:
1. Нажмите иконку "стрелка в квадрате" (обычно в левом верхнем углу DevTools)
2. ИЛИ нажмите `Ctrl + Shift + C`

Курсор мыши теперь будет подсвечивать элементы на странице!

## Шаг 4: Найдите нужный элемент

### Пример 1: Найти кнопку "Sign Up"

1. Наведите курсор на кнопку "Sign Up"
2. Элемент подсветится синим
3. Кликните по кнопке
4. В DevTools откроется HTML код этой кнопки:

```html
<button class="btn-primary" data-testid="signup-button">Sign Up</button>
```

### Что это значит?
- Это КНОПКА (`button`)
- У неё есть класс `btn-primary`
- У неё есть атрибут `data-testid` со значением `signup-button`
- Текст внутри: "Sign Up"

### Какой селектор использовать?

**Вариант 1 (лучший):**
```python
By.CSS_SELECTOR, "[data-testid='signup-button']"
```

**Вариант 2:**
```python
By.CSS_SELECTOR, "button.btn-primary"
```

**Вариант 3:**
```python
By.XPATH, "//button[text()='Sign Up']"
```

### Пример 2: Найти поле для email

1. Наведите курсор на поле "Email"
2. Кликните
3. В DevTools увидите что-то вроде:

```html
<input
    type="email"
    name="userEmail"
    id="email-input"
    placeholder="Enter your email"
    class="form-control"
/>
```

### Какой селектор использовать?

**Вариант 1 (самый надежный - по ID):**
```python
By.ID, "email-input"
```

**Вариант 2 (по name):**
```python
By.NAME, "userEmail"
```

**Вариант 3 (по типу):**
```python
By.CSS_SELECTOR, "input[type='email']"
```

## Шаг 5: Скопировать селектор автоматически

### Способ A: Копирование через DevTools

1. Кликните на элемент в DevTools (HTML код)
2. Правой кнопкой мыши на этом элементе
3. "Copy" → "Copy selector" (скопирует CSS селектор)
4. ИЛИ "Copy" → "Copy XPath" (скопирует XPath)

Например, скопируется:
```
#root > div > div.main-content > form > input[type="email"]
```

Используйте так:
```python
By.CSS_SELECTOR, "#root > div > div.main-content > form > input[type='email']"
```

### Способ B: Проще (используйте уникальные атрибуты)

Вместо длинного пути, ищите:
1. **id** - если есть, это лучший вариант
2. **data-testid** или **data-test** - специально для тестов
3. **name** - для полей ввода
4. **class** - если уникальный
5. **текст** - для кнопок и ссылок

## Шаг 6: Проверить селектор в консоли

В DevTools перейдите на вкладку "Console" и введите:

```javascript
// Проверить CSS селектор:
document.querySelector("[data-testid='signup-button']")

// Проверить XPath:
$x("//button[text()='Sign Up']")
```

Если вернулся элемент - селектор правильный! ✅
Если вернулся `null` - селектор неправильный ❌

## Практический пример: Замена селекторов в коде

### БЫЛО (в моем коде):
```python
# Шаг 3: Нажать Sign Up
self.find_and_click(By.XPATH, "//button[contains(text(), 'Sign Up')]")  # TODO
```

### ВЫ НАШЛИ в DevTools:
```html
<button data-testid="signup-btn" class="signup-button">Sign Up</button>
```

### СТАЛО (после замены):
```python
# Шаг 3: Нажать Sign Up
self.find_and_click(By.CSS_SELECTOR, "[data-testid='signup-btn']")
```

## Какие элементы нужно найти для Betr?

Вам нужно найти селекторы для:

### На странице Sign Up:
1. ✅ **Кнопка "Sign Up"** (первая кнопка для открытия формы регистрации)
2. ✅ **Поле Email**
3. ✅ **Поле Password**
4. ✅ **Кнопка "Next"** (после email/password)

### На странице с номером телефона:
5. ✅ **Поле Phone Number**
6. ✅ **Чекбокс "Enable Biometrics"** (его нужно СНЯТЬ)
7. ✅ **Чекбокс "Terms and Conditions"** (его нужно ПОСТАВИТЬ)
8. ✅ **Кнопка "Next"**

### На странице SMS кода:
9. ✅ **Поле для ввода SMS кода**

### На странице даты рождения:
10. ✅ **Поле Year (год)**
11. ✅ **Поле Month (месяц)**
12. ✅ **Поле Day (день)**
13. ✅ **Кнопка "Apply"**
14. ✅ **Кнопка "Next"**

### На странице SSN:
15. ✅ **Поле SSN (последние 4 цифры)**
16. ✅ **Кнопка "Next"**

### На странице имени:
17. ✅ **Поле First Name**
18. ✅ **Поле Last Name**
19. ✅ **Кнопка "Next"**

### На странице адреса:
20. ✅ **Поле Address**
21. ✅ **Первый элемент в выпадающем списке адресов**
22. ✅ **Кнопка "Next"**

### На последней странице:
23. ✅ **Чекбокс 1** (согласие 1)
24. ✅ **Чекбокс 2** (согласие 2)
25. ✅ **Чекбокс 3** (согласие 3)
26. ✅ **Чекбокс 4** (согласие 4)
27. ✅ **Кнопка "Verify Identity"**

## Советы:

### ✅ Хорошие селекторы (используйте):
- `data-testid="something"` - специально для автоматизации
- `id="unique-id"` - уникальный идентификатор
- `name="fieldName"` - для полей форм

### ❌ Плохие селекторы (избегайте):
- Длинные цепочки классов
- Позиционные селекторы типа `div:nth-child(3)`
- Селекторы зависящие от структуры страницы

## Если застряли - спросите меня!

Скажите:
"Не могу найти селектор для поля Email"

И я помогу найти альтернативные способы!

## Быстрая шпаргалка:

| Что найти | Как найти |
|-----------|-----------|
| Кнопка | Ищите `<button>` или `<a>` с нужным текстом |
| Поле ввода | Ищите `<input>`, смотрите на `name=` или `id=` |
| Чекбокс | Ищите `<input type="checkbox">` |
| Выпадающий список | Ищите `<select>` или `<div>` с классами типа `dropdown` |

## Пример заполненного кода:

```python
# Вместо:
self.type_text(By.NAME, "email", account.email)  # TODO

# Вы напишете (после того как найдете):
self.type_text(By.ID, "registration-email-field", account.email)
```

Готово! Теперь открывайте сайт Betr и находите селекторы! 🚀
