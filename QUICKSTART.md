# 🚀 Быстрый старт бота

## Шаг 1: Подготовка (ВАЖНО!)

**Отправьте /start боту @umartaufabot в Telegram**

Это необходимо для того, чтобы бот мог отправлять вам уведомления.

## Шаг 2: Установка зависимостей

```bash
pip3 install requests
```

## Шаг 3: Тестирование

```bash
python3 test_bot.py
```

Если всё прошло успешно, вы увидите:
```
✓ Все тесты пройдены! Бот готов к запуску.
```

## Шаг 4: Запуск бота

### Вариант 1: Однократный поиск (для теста)
```bash
python3 cheap_flights_bot.py --once
```

### Вариант 2: Непрерывная работа (проверка каждый час)
```bash
python3 cheap_flights_bot.py
```

### Вариант 3: Через bash-скрипт
```bash
./start_flight_bot.sh
```

## Что будет искать бот?

- 📍 Маршрут: **Москва → Уфа**
- 📅 Даты:
  - 26 декабря 2024 (вечер, с 18:00)
  - 27 декабря 2024 (любое время)
  - 28 декабря 2024 (любое время)
- 💰 Максимальная цена: **10,000 ₽**
- ⏰ Проверка: **каждый час**

## Изменение настроек

Откройте файл `flight_config.py` и измените нужные параметры:

```python
# Изменить маршрут
ORIGIN_CITY = "LED"  # Санкт-Петербург
DESTINATION_CITY = "MOW"  # Москва

# Изменить максимальную цену
MAX_PRICE = 5000  # рублей

# Изменить интервал проверки
CHECK_INTERVAL = 1800  # 30 минут (в секундах)
```

## Логи

Все действия бота записываются в файл `cheap_flights_bot.log`:

```bash
# Последние 50 строк
tail -50 cheap_flights_bot.log

# Непрерывный просмотр
tail -f cheap_flights_bot.log
```

## Остановка бота

Нажмите `Ctrl+C` в терминале, где запущен бот.

## Помощь

- Полная документация: `FLIGHT_BOT_README.md`
- Telegram бот: @umartaufabot
- Токен: `8080110045:AAGK01_8PByIWA-F9o4wJnlGRdQtWu89Uyo`

## Проблемы?

1. Убедитесь, что вы отправили /start боту в Telegram
2. Проверьте логи: `tail -50 cheap_flights_bot.log`
3. Запустите тест: `python3 test_bot.py`
